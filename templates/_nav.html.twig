<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="#">
        <img src="/image/logoFantastic.png" width="80" height="40" alt="logo des éditions Fantastic">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{path('home')}}">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{path('univers')}}">Univers Fantastic</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Nos livres
                </a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <li><a class="dropdown-item" href="{{path('catalogue')}}">Tous nos livres</a></li>
                    <li><a class="dropdown-item" href="{{path('ageName')}}">Age</a></li>
                    <li><a class="dropdown-item" href="{{path('serieName')}}">Collection</a></li>
                    <li><a class="dropdown-item" href="{{path('themeName')}}">Thème</a></li>
                    <li><a class="dropdown-item" href="{{path('genreName')}}">Genre</a></li>
                    
                </ul>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{path('nosAuteurs')}}">Nos auteurtrices</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{path('nosIllustrateurs')}}">Nos illustrateurtrices</a>
            </li>
            {# icône du panier#}
            <li class="nav-item">
                <a class="nav-link" href="#">
                    <i class="fas fa-shopping-cart"></i>
                </a>
            </li>
                {% if app.user %}
                    {#si l'utilisateur est connecté, il verra #}
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{path('app_mon_compte')}}">Mon compte</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{path('app_admin')}}">Back office</a>
                        </li>
                        {#<li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{path('app_livre_index')}}">Gestion des produits</a>
                        </li>
                        app_user_index

                        #}



                    {% endif %}

                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{path('app_logout')}}">Déconnexion</a>
                    </li>

                {% else %}
                    {#si l'utilisateur n'est pas connecté, il verra #}
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{path('app_register')}}">Inscription</a>
                    </li>
                    
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{path('app_login')}}">Connexion</a>
                    </li>
                    
                {% endif %}

            
                                
        </ul>
    </div>
</nav>
