{% extends 'base.html.twig' %}

{% block title %}AdresseFacture{% endblock %}

{% block body %}
    <h1>AdresseFacture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adresse_facture.id }}</td>
            </tr>
            <tr>
                <th>RueFacture</th>
                <td>{{ adresse_facture.rueFacture }}</td>
            </tr>
            <tr>
                <th>VilleFacture</th>
                <td>{{ adresse_facture.villeFacture }}</td>
            </tr>
            <tr>
                <th>CodePostalFacture</th>
                <td>{{ adresse_facture.codePostalFacture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adresse_facture_index') }}">back to list</a>

    <a href="{{ path('app_adresse_facture_edit', {'id': adresse_facture.id}) }}">edit</a>

    {{ include('adresse_facture/_delete_form.html.twig') }}
{% endblock %}
