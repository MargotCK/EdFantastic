<form method="post" action="{{ path('app_adresse_livraison_delete', {'id': adresse_livraison.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette adresse ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adresse_livraison.id) }}">
    <button class="btn btn-danger mt-2 ">Supprimer</button>
</form>
