{% extends 'base.html.twig' %}

{% block title %}AdresseLivraison{% endblock %}
{% block h1 %}{{ adresse_livraison.id }}{% endblock %}
{% block body %}
    

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adresse_livraison.id }}</td>
            </tr>
            <tr>
                <th>RueAdresseLivraison</th>
                <td>{{ adresse_livraison.rueAdresseLivraison }}</td>
            </tr>
            <tr>
                <th>VilleAdresseLivraison</th>
                <td>{{ adresse_livraison.villeAdresseLivraison }}</td>
            </tr>
            <tr>
                <th>CodePostalLivraison</th>
                <td>{{ adresse_livraison.codePostalLivraison }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ adresse_livraison.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('app_adresse_livraison_index') }}">Retour</a>

    <a class="btn btn-primary" href="{{ path('app_adresse_livraison_edit', {'id': adresse_livraison.id}) }}">    <a class="btn btn-primary" href="{{ path('app_adresse_facture_edit', {'id': adresse_facture.id}) }}">Modifier</a>
</a>

    {{ include('adresse_livraison/_delete_form.html.twig') }}
{% endblock %}
