{% extends 'base.html.twig' %}

{% block title %}Auteur{% endblock %}
{% block h1 %}{{ auteur.id }}{% endblock %}

{% block body %}
    

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auteur.id }}</td>
            </tr>
            <tr>
                <th>Prenom, Nom</th>
                <td>{{ auteur.prenom }}</td>
            </tr>
            
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('app_auteur_index') }}">Retour</a>

    <a class="btn btn-primary" href="{{ path('app_auteur_edit', {'id': auteur.id}) }}">Modifier</a>

    {{ include('auteur/_delete_form.html.twig') }}
{% endblock %}
