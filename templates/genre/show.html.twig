{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}
{% block h1 %}{{ genre.id }}{% endblock %}
{% block body %}
   

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ genre.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('app_genre_index') }}">Retour</a>

    <a class="btn btn-primary" href="{{ path('app_genre_edit', {'id': genre.id}) }}">Modifier</a>

    {{ include('genre/_delete_form.html.twig') }}
{% endblock %}
