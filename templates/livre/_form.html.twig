 {{form_start(formLivre)}}
            
    {{form_row(formLivre.titre)}}
    {{form_row(formLivre.ean)}}
    {{form_row(formLivre.prix)}}
    {{form_row(formLivre.quantiteStock)}}
    {{form_row(formLivre.couv1)}}
    {{form_row(formLivre.serie)}}
    
    <input type="submit" value="ajouter" class="btn btn-primary w-100">
{{form_end(formLivre)}}
