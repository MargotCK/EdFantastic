{% extends 'base.html.twig' %}

{% block title %}Serie{% endblock %}
{% block h1 %}{{ serie.id }}{% endblock %}

{% block body %}
    

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ serie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ serie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('app_serie_index') }}">back to list</a>

    <a class="btn btn-primary" href="{{ path('app_serie_edit', {'id': serie.id}) }}">edit</a>

    {{ include('serie/_delete_form.html.twig') }}
{% endblock %}
